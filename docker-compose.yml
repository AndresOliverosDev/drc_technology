services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: drc_technology_web
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
